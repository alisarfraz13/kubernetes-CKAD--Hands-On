apiVersion: v1
kind: Pod
metadata:
  name: test-pod-3
spec:
  securityContext:
    #runAsNonRoot: true        # <-- K8s check karega ke container root par na chale
    runAsUser: 1000
  containers:
  - name: ubuntu-container-1
    image: ubuntu
    command: [ "sleep", "5000"]
    securityContext:
      runAsUser: 1008
