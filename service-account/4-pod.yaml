apiVersion: v1
kind: Pod
metadata:
  name: cluster-checker-pod
  namespace: default
spec:
  serviceAccountName: pod-reader-sa
  containers:
  - name: k8s-cli-container
    image: bitnami/kubectl:latest
    command: ["sh", "-c", "while true; do kubectl get pods; sleep 10; done"]
