apiVersion: v1
kind: Pod
metadata:
  name: web-pod
  labels:
    web: app
 namespace: default
spec:
  container:
  - name: test-container
    image: kodekloud/webapp-color
    envFrom:
    - configMapRef:
        name: web-config
