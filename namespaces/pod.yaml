apiVersion: v1
kind: Pod
metadata:
  name: al-nafi-pod-3
  namespace: al-nafi
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests:
        memory: "1Gi"
        cpu: "500m"
      limits:
        memory: "1.5Gi"
        cpu: "2"
